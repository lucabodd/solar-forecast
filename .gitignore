# Ignore binary in root (old location)
/solar-forecast

# Build artifacts (except bin/ directory which is committed)
*.log
.DS_Store
state/

# Configuration files with credentials
config/application.properties
config/*.user
.env
*.key
*.pem

# Sensitive data
*credentials*
*secret*
